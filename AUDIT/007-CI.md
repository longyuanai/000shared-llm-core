# 007-CI validation record

**Date:** 2026-07-25
**Status:** PASS-WITH-NITS

## Scope

This record covers `.github/workflows/inspect.yml`, the README status badge,
the six explicit Windows pytest steps, and the existing
`scripts/inspect_worker_return.py` four-part inspection.

## Test cases

| Case | Result | Evidence |
|---|---|---|
| Positive six-worker inspection | PASS (local) | The existing inspector returned `OVERALL PASS` with all six workers green. |
| Negative commit-history inspection | PASS (local simulation) | A disposable one-commit repository was rejected by `check_commits`; no product repository was reset or modified. |
| Windows Chinese-path pytest/basetemp | PASS (local) | The inspector ran all six product suites, and the core suite separately returned `105 passed`; all used external `C:/pytest-tmp/...` basetemps and `-o addopts=`. |

## Workflow checks

- YAML parsing: PASS.
- Six explicit pytest steps: PASS.
- `windows-latest`: PASS.
- Absolute Python resolution through `(Get-Command python).Source`: PASS.
- Per-worker `--basetemp=C:/pytest-tmp/<worker>/`: PASS.
- Per-worker `-o addopts=`: PASS.
- `actionlint` v1.7.12: PASS with no findings.
- GitHub-hosted `workflow_run`: not run because this local repository has no
  configured remote.
- Deliberately broken hosted-runner check: not run for the same reason.

## Nits and follow-up

The dispatch describes a mono-ish repository rooted above all seven local
repositories, while the implementation request places the workflow in
`000shared-llm-core`. The workflow therefore preserves the documented
mono-ish checkout layout. Before enabling the required branch check, configure
the GitHub repository so its checkout contains the six product directories and
the root `scripts/inspect_worker_return.py`, then run the positive and negative
smokes on a test branch.
