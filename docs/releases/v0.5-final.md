# longyuanai v0.5 / M2.1 final validation report

> Original v0.5 validation: 2026-07-25
>
> M2.1 closure validation: 2026-08-06
>
> Post-publication security and CI follow-up: 2026-08-07
>
> Scope: `shared-llm-core`, integration, products 001 through 006, and `web-ui`
>
> Result: **PASS for M2.1 engineering validation and preproduction Gateway acceptance
> (local, remote CI, private-image publication, and Render/Neon live E2E); production-grade
> hosting, operational readiness, and production traffic acceptance remain open**
>
> Security update: all API keys found active in the persistent local `local-dev` Gateway
> store were revoked on 2026-08-07. The separate short-lived key issued for the Render/Neon
> acceptance run was also revoked after the tests, and its `revoked_at` state was verified
> directly in Neon. See "Leaked E2E token" for the historical evidence boundary.

## M2.1 closure

### Locked web UI delivery

- `web-ui`: `3f814afb9bf26d37f3fa9b7d175c7bd2e40d372f`
- Playwright now derives and validates demo/live server environment during config loading.
- Cloudflare Workerd receives the non-secret environment whitelist through inline `vars`; only `GATEWAY_TOKEN` is declared as a required secret.
- `nodejs_compat` is preserved and `nodejs_compat_populate_process_env` is enabled.
- E2E setup no longer writes `.env.local`, has no module-level setup side effects, and does not configure the ineffective same-file named teardown.
- Playwright does not reuse an existing dev server, avoiding cross-mode test contamination.
- Web UI CI and the suite lock now target Node 22, matching the package engine requirement.

### M2.1 web UI verification

| Check | Result |
|---|---:|
| `npm run lint` | PASS |
| `npm run typecheck` | PASS |
| `npm test` | 3 passed, 0 failed; production build completed |
| Demo Playwright, Chromium | 6 passed, 0 failed |
| Demo Playwright, Mobile Chromium | 6 passed, 0 failed |
| Live Playwright, Chromium | 6 passed, 0 failed |
| Live Playwright, Mobile Chromium | 6 passed, 0 failed |
| Live-mode missing-variable guard | PASS; rejected before server startup |
| Token/artifact scan | PASS **for the working tree, reachable history, and generated artifacts only** — see "Leaked E2E token" below for the orphaned-commit and credential status |

The live suite used an authorized local IntegrationGateway. It covered dashboard rendering,
deep links, BFF scan flows, event streaming, and token-isolation assertions. The temporary
gateway process was stopped after validation, its test token was removed from the shell
environment, and no `web-ui/.env.local` remained.

### Remote CI status

- [`suite-ci` run `31162707650`](https://github.com/longyuanai/000shared-llm-core/actions/runs/31162707650)
  completed successfully at core head `afe2e7dbecbd789081211eb556eaf258264ccfed`.
  The Python 3.12 runner used `actions/checkout@v7` and `actions/setup-python@v7`, checked
  out and lock-verified all nine repositories, and produced core (`103 passed, 2 skipped`),
  IntegrationGateway (`96 passed, 2 skipped`), 001 (`274 passed`), 002 (`178 passed`),
  003 (`373 passed`), 004 (`177 passed, 1 warning`), 005 (`278 passed`), and 006
  (`392 passed, 1 skipped`): **1871 passed, 5 skipped, 1 warning, 0 failed** in total.
- [`publish-gateway-image` run `31162850945`](https://github.com/longyuanai/000shared-llm-core/actions/runs/31162850945)
  completed successfully at the same core head. It checked the locked image source set,
  rejected local artifacts from the source image, built the full Python 3.12 runtime for
  `linux/amd64`, generated SBOM and max-mode provenance attestations, and published the
  private image described under "Preproduction Gateway acceptance". This remote build is
  the accepted runtime-build record; earlier local Docker/PyPI timeouts are not counted as
  passes.
- `web-ui-ci` run `31100752510` completed successfully for locked head `3f814af`.
  This independently verifies the private repository's Node 22 lint, typecheck, build,
  Node tests, and configured demo E2E job in GitHub Actions.
- Earlier [`suite-ci` run `31123850926`](https://github.com/longyuanai/000shared-llm-core/actions/runs/31123850926)
  completed successfully for core head `30892812ac9649228cf18b0ecc18314be48aa5b2`.
  It checked out all nine locked repositories, including private `web-ui`; verified every
  locked SHA; installed all Python projects; and ran every Python suite from its own
  repository root. Results were core (`103 passed, 2 skipped`), IntegrationGateway
  (`93 passed, 2 skipped`), 001 (`274 passed`), 002 (`178 passed`), 003 (`373 passed`),
  004 (`177 passed, 1 warning`), 005 (`278 passed`), and 006 (`392 passed, 1 skipped`):
  **1868 passed, 5 skipped, 1 warning, 0 failed** in total.
- Earlier run `31120786639` found the 004 CPython ABI fixture defect. Run `31121452216`
  attempt 2 then passed 175 of 177 004 tests and exposed the fixed-parent-depth suite-root
  assumption. Locked 004 head `e500dab` resolves both issues by validating the active ABI
  and discovering the ancestor containing `000shared-integration/src`.
- Run `31121452216` attempt 1 at core head `594d569` failed in GitHub's `Set up job`
  before any checkout or repository command ran. At that time
  [GitHub's official incident](https://www.githubstatus.com/incidents/qcvjkzcs7j74)
  classified Actions as a critical major outage. Attempt 2 subsequently completed every
  checkout, lock-verification, setup, and installation step and produced the actionable
  004 suite-root failure described above. These historical failures are superseded for
  release validation by successful runs `31123850926` and `31162707650`; they remain
  recorded as remediation evidence rather than being relabelled as passes.

### Security and deployment status

#### Preproduction Gateway acceptance — VERIFIED (2026-08-07)

- Locked container sources were `000shared-llm-core` at `afe2e7d` and
  `000shared-integration` at `e551c1a`. The published private image is
  `ghcr.io/longyuanai/integration-gateway:m21-preprod`, pinned for deployment by digest
  `sha256:a96b285dbdc7cde59199a4b90612b837cb715f351e3ac56d5508e5237af670f5`.
  GitHub package settings were checked after publication and reported the package as
  private.
- The digest-pinned image was deployed on a Render Free web service in Frankfurt with
  authentication required, database-backed identity, inline jobs, and `/livez` as the
  health check. The accepted HTTPS endpoint is
  `https://longyuanai-m21-gateway.onrender.com`; Render reported the service live and
  repeated `/livez` requests returned HTTP 200. Schema auto-creation was enabled only for
  the first Neon bootstrap, then changed to `INTEGRATION_AUTO_CREATE_SCHEMA=false`; the
  resulting Render redeploy (`dep-d9qte11t0dsc738ml210`) also reached the primary URL and
  continued returning HTTP 200 from `/livez`.
- Persistence used Neon Free PostgreSQL project `curly-mode-59745692` in Frankfurt.
  The connection string and database password were supplied only as a masked Render
  environment variable and one-time local process input; neither value is stored in Git
  or this report.
- A dedicated `e2e-m21-20260807a` tenant received one admin key scoped to `gateway:*`,
  expiring at `2026-08-07T14:40:13Z`. Against the Render HTTPS endpoint, the live
  Playwright suite passed Chromium (`6 passed`) and Mobile Chromium (`6 passed`):
  **12 passed, 0 failed**. Trace capture was disabled and the output directory was outside
  the worktree.
- The key was revoked immediately after the suite. An independent read-only Neon query
  then returned `issued_keys=1` and `revoked_keys=1` for that tenant. The process
  environment was restored, the temporary Neon credential file and Playwright output
  directory were removed, and the existing ignored `web-ui/test-results/.last-run.json`
  remained unchanged from 2026-08-06 and contained no token pattern.
- Render pulls the private image through a dedicated GitHub classic PAT limited to
  `read:packages`, expiring 2026-08-14. The credential is stored only in Render's private
  registry credential store. It must be rotated or replaced before expiry if the
  preproduction service is retained.

This closes M2.1 **preproduction** deployment acceptance, not production release
acceptance. Render Free can cold-start and carries no production SLA; the Neon project,
Render service, test tenant, and short-lived key were created solely for this acceptance
environment. Production-grade capacity, monitoring, backup/restore proof, production
credentials, an approved test window, and sustained traffic evidence remain separate.

#### Leaked E2E token — three separate facts, do not conflate them

The previously hardcoded `igw_`-prefixed gateway token (originally in
`web-ui` commit `e40fca7`, `tests/e2e/global-setup.ts:26`) has three independent
statuses. Clearing one does not clear the others.

1. **Reachable history — VERIFIED CLEAN (2026-08-06).** A full scan of
   `git rev-list --all` across `web-ui` returned zero matches, as did the same scan
   over `000shared-llm-core` and `000shared-integration`. No branch-reachable commit
   in the suite contains the token.
2. **Local object store — CLEARED (2026-08-06).** Orphaned commit `e40fca7` had survived
   the branch rewrite: unreachable from every ref, but still reflog-reachable and still
   listed by `git fsck`, and therefore recoverable with a single `git log -g` by anyone
   with filesystem access to the checkout. It was removed with
   `git reflog expire --expire=now --expire-unreachable=now --all` followed by
   `git gc --prune=now --aggressive`.

   Pre-flight before the prune: all four orphaned commits (`0737d1c`, `469cd99`,
   `b3c2d35`, `e40fca7`) were tree-diffed against `HEAD`. The only path present in them
   and absent from `HEAD` was `tests/e2e/global-setup.ts` — the file this milestone
   deliberately deleted — so the prune discarded no unmerged work.

   Post-conditions verified: `git cat-file -t e40fca7` → `fatal: Not a valid object name`;
   `git fsck --unreachable --dangling` → empty; token scan across
   `git rev-list --all --reflog` → zero matches; `git status` clean; branch still six
   commits with `HEAD` unchanged at `3f814af`, so the `suite-lock.yml` web_ui pin
   remains valid.
3. **Credential — ACTIVE LOCAL KEYS REVOKED (2026-08-07).** An independent read-only
   query of the persistent IntegrationGateway Postgres store found two active, unrevoked
   `local-dev` admin keys. Port 3100 had no Dashboard listener. Both keys were revoked
   through `shared-integration-admin api-key-revoke` with actor
   `codex-m2.1-rotation`; a post-change database query returned `revoked=true` for both,
   and `audit_events` contains two matching `api_key.revoked` / `success` records.

   The original plaintext token was deliberately not recovered after the Git object prune,
   so an exact plaintext-to-row mapping was not re-established. Revoking every active key in
   the audited persistent `local-dev` store closes the live credential path in that store;
   it is not evidence about an unexamined backup or a different external Gateway. No
   replacement key was issued because there was no current consumer. The next authorized
   local live E2E run must issue a new short-lived key and remove it after the run.

   **History cleanup is not revocation:** items 1 and 2 removed recoverable copies of the
   string, while item 3 changed authentication state. All three controls were required.

Propagation note: with item 2 complete the object no longer exists locally, so the first
push of `web-ui` cannot carry it to the remote by any push mode. This was already true
for an ordinary branch push before the prune (unreachable objects are not transferred);
the prune additionally closes the `git push --mirror` / `--all` case and removes the
local-filesystem recovery path.

**Confirmed after publication (2026-08-06).** `web-ui` has since been pushed, and the
remote was checked against the propagation claim above:

- `repos/longyuanai/web-ui/commits` → exactly 6 commits, identical to the local branch.
- `repos/longyuanai/web-ui/commits/e40fca7…` → HTTP 422, "No commit found for SHA".
- `repos/longyuanai/web-ui/contents/tests/e2e` → `dashboard.spec.ts` and
  `live-dashboard.spec.ts` only; `global-setup.ts` is absent.

These checks confirm that the leaked commit and file did not reach the published GitHub
repository: `web-ui` had no remote configured while the leak existed, and its first
publication happened after the prune. This conclusion is limited to the GitHub repository
and the explicitly scanned working tree, Git object store, and generated test artifacts; it
does not claim that every external backup, terminal capture, or process log was audited.

- The earlier absence of a remotely reachable Gateway is closed for the Render/Neon
  preproduction environment above. A production-grade `GATEWAY_URL`, production
  credentials, and a production acceptance window remain external release blockers and
  are not stored in the suite.
- **Publication status (superseded — this section originally read "no push was
  performed").** `web-ui` was published on 2026-08-06 to `longyuanai/web-ui`, created
  **private**, with `main` at `3f814af` — matching the `suite-lock.yml` web_ui pin, so no
  lock update was required. It is the ninth and last locked repository to reach GitHub;
  the other eight are public. `000shared-llm-core` was subsequently pushed and now carries
  the nine-repository checkout and lock-validation workflow.
- **`SUITE_READ_TOKEN` is now a hard dependency of `inspect.yml`.** Before `web-ui` existed
  the workflow's `secrets.SUITE_READ_TOKEN || github.token` fallback always resolved to
  `github.token`, and it worked only because all seven checked-out repositories were
  public. `web-ui` is private, so cross-repository checkout of it cannot fall back —
  the secret must be present or the "Check out web UI" step fails with a 401.
  The secret was configured on `longyuanai/000shared-llm-core` on 2026-08-06 using a
  fine-grained token scoped to `web-ui` only with `Contents: Read-only`, issued **with no
  expiration**. Rationale and residual risk: a non-expiring credential cannot silently
  break CI later, and the fine-grained scope keeps the blast radius to read-only access to
  one private repository. The accepted trade-off is that nothing forces a periodic review,
  so this token must be rotated manually. It has no automatic review date.
- The pre-existing seven modified files and untracked `tests/test_tenant_isolation.py` in
  `003AI Agent安全靶场` were left untouched.

## Delivered issues

1. **005-FINAL-001** — six-product real CLI envelope smoke tests.
2. **005-FINAL-002** — auditable six-worker four-part validation evidence.
3. **005-FINAL-003** — RFC-001 API and compatibility freeze.
4. **005-FINAL-004** — final regression, release report, and status index.

## Verification commands

### Core and cross-repository tests

```powershell
& 'C:\Users\15072\AppData\Local\Programs\Python\Python314\python.exe' `
  -m pytest tests/ `
  --basetemp=C:/pytest-tmp/005-final-core `
  -q --tb=short -o addopts=
```

Result:

```text
105 passed in 7.61s
```

### Six-worker four-part inspection

```powershell
& 'C:\Users\15072\AppData\Local\Programs\Python\Python314\python.exe' `
  'E:\001项目\000开发\003AI+网络安全\scripts\inspect_worker_return.py'
```

Result:

```text
Worker A · 001-S4   PASS
Worker B · 002-S4   PASS
Worker C · 003-S4   PASS
Worker D · 004-S4   PASS
Worker E · 005-S4   PASS
Worker F · 006-S5   PASS
OVERALL             PASS  6/6 workers green
```

The four checks are recent commits, `HEAD~2` diffstat, isolated-basetemp pytest,
and a real subprocess CLI JSON envelope.

## Test totals

| Scope | Result |
|---|---:|
| shared-llm-core, including six cross-repository smoke cases | 105 passed |
| 001 AI-SOC-Agent | 76 passed |
| 002 AI-Vulnerability-Agent | 91 passed, 1 warning |
| 003 AI-Agent-Security-Lab | 170 passed |
| 004 AI-CodeGuard-upgrade | 74 passed |
| 005 AI-Reverse-Agent | 167 passed, 1 warning |
| 006 AI-Firmware-Security-Agent | 116 passed, 1 skipped |
| **Combined** | **799 passed, 1 skipped, 0 failed** |

## Frozen artifacts

- `docs/v0.5-contract.md`
- `docs/rfcs/RFC-001-v0.5-freeze.md`
- `tests/test_contract_examples.py`
- `tests/test_cli_envelope_contract.py`
- `tests/integration/test_cli_envelope_smoke.py`
- `docs/validation/005-final-worker-return.md`

## Constraint audit

- Post-publication CI remediation changed only verified compatibility contracts in
  IntegrationGateway, 003, and 004; no unrelated product business behavior was changed.
- The pre-existing seven tracked modifications and untracked
  `tests/test_tenant_isolation.py` in the original 003 worktree were not staged, modified,
  committed, or cleaned. The 003 CI fixes were made in an independent worktree.
- No v0.1 §1-§6 API was changed.
- No production dependency was added.
- No API key, token, password, private key, or customer data was added.
- All Python commands used the required absolute interpreter.
- All pytest commands used `C:/pytest-tmp/...` and `-o addopts=`.

## Remaining work outside this freeze

- `007-CI` and `007-CI-FIX` are complete; the four-part validation now runs in the
  nine-repository suite workflow.
- The GitHub Actions Runtime follow-up is complete: `actions/checkout` and
  `actions/setup-python` use official major `v7`, whose action runtime is Node 24. This is
  independent of the web-ui Node 22 project runtime. Run `31162707650` is the accepted
  nine-repository record for the upgraded actions and Python 3.12 suite.
- The Web Dashboard is locked, engineering-validated, and accepted against the
  Render/Neon preproduction Gateway at M2.1. Production-grade deployment, production
  credentials, and production traffic acceptance remain separate release work.
- The next unlocked work is `docs/dispatches/008-M2-OPS.md`: backup/restore evidence
  and real PostgreSQL concurrency validation. Local execution passed on 2026-08-07;
  Integration commit `4001790` and web-ui commit `e5a5274` were pushed and locked for
  the follow-up suite CI. Authentication, tenant-aware UI, capacity, monitoring, and
  continuous traffic remain later release work.
- v1.0 still requires seven v1.0 products and 30 days of production gateway traffic.
