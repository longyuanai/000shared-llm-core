# Codex 派活指令 · 002-S4 · AI-Vulnerability-Agent v0.6 IntegrationGateway adapter

> **派活方**: Claude
> **接收方**: Codex
> **前提**: 005-INTEG 已 done、002 v0.5 S3 已 done(68 passed)
> **优先级**: 🟢 Week 11

---

## ⚠️ 开工前必读(6 个文件)

1. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\adapters\base.py` —— **adapter 契约**
2. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\adapters\vuln.py` —— VulnAdapter 当前实现
3. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\gateway.py`
4. `E:\001项目\000开发\003AI+网络安全\000shared-llm-core\docs\v0.5-contract.md` §7
5. `E:\001项目\000开发\003AI+网络安全\002AI-Vulnerability-Agent\docs\tech-spec.md`
6. `E:\001项目\000开发\003AI+网络安全\002AI-Vulnerability-Agent\docs\TODO.md`

## ⚠️ Windows 踩坑经验

`E:\001项目\000开发\003AI+网络安全\AUDIT\EXPERIENCE.md` —— **经验 1 + 2 + 4**

---

## 工作目录

```
E:\001项目\000开发\003AI+网络安全\002AI-Vulnerability-Agent
```

## 当前状态

- Qualys/OpenVAS CSV 解析器 + PRisk 公式 + 拓扑感知 + 68 tests
- **缺**: CLI 子命令 `--input --json` 适配 JSONSubprocessAdapter 契约

---

## 本 Stage 目标(2 个 ISSUE / 2 个 commit)

### ISSUE 1 · VulnAdapter CLI 契约适配(VULN-CLI-001)

**目标**: 让 `python -m ai_vuln_agent.cli scan --input {...} --json` 被 adapter 调用,输出 envelope。

**任务**:
1. 查 `src/ai_vuln_agent/__main__.py` 或 cli.py 现状
2. 新增 `python -m ai_vuln_agent.cli scan --input <payload.json> --json`:
   - `--input` 接 JSON:`{"scanner": "qualys|openvas|nessus", "csv_content": "..."}` 或文件路径
   - 支持 `--csv-file <path>`(真实 CSV 文件)
   - `--json` 输出 envelope
3. 输出 envelope(必含字段对齐 Finding):
```json
{
  "findings": [
    {"id": "vuln-CVE-2024-1234", "severity": "critical", "confidence": 0.95,
     "title": "CVE-2024-1234 in OpenSSL", "cve": "CVE-2024-1234", "host": "10.0.0.1",
     "narrative": "PRisk 9.2: CVSS 9.8 + EPSS 0.8 + KEV"}
  ]
}
```

**测试**(≥ 3 个):
- `test_cli_envelope.py::test_scan_qualys_csv`
- `test_cli_envelope.py::test_scan_openvas_csv`
- `test_cli_envelope.py::test_prisk_in_narrative`
- 复用 `JSONSubprocessAdapter` end-to-end

**验收**:
- `echo '{"scanner":"qualys","csv_content":"..."}' | python -m ai_vuln_agent.cli scan --json` → 合法 JSON
- pytest 全过
- 1 个 commit

### ISSUE 2 · 真实 NVD key 支持 + 集成(VULN-LIVE-001)

**目标**: 让 `NVD_API_KEY` 环境变量被尊重(可选),跑通 `/v0.5/vuln/scan` 端到端。

**任务**:
1. `src/ai_vuln_agent/nvd.py` 加 NVD_API_KEY 读取(无 key 时 fallback 到 mock)
2. `tests/integration/test_vuln_adapter_e2e.py`:
   - 启 gateway 端口 18080,POST `/v0.5/vuln/scan` body `{"scanner":"qualys",...}`
   - 验证响应 + 跑 `/v0.5/health`
3. README 加章节"NVD API key 使用"

**测试**(≥ 3 个):
- `test_vuln_adapter_e2e.py::test_post_scan_returns_findings`
- `test_vuln_adapter_e2e.py::test_health_endpoint_reports_vuln_ok`
- `test_nvd.py::test_env_api_key_override`

**验收**:
- `python -m 000shared-integration.gateway` 起来后 `curl /v0.5/health` 全 ok
- pytest 全过
- 1 个 commit

---

## 约束

- 不修改 `000shared-llm-core` / `000shared-integration`
- 不破坏 v0.5 S3 的 68 测试
- 不装新依赖
- Windows 兼容,pytest 三件套

---

## 回报(每 ISSUE)

```
1. Files changed
2. Tests: X/X passed(原 68 + 新增 ≥ 6)
3. CLI smoke 输出
4. Integration health curl 输出
5. Deviations
```

## 最终回报

- 2 commit hash
- 总测试数
- gateway 联调输出