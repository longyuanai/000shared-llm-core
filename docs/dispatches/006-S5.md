# Codex 派活指令 · 006-S5 · AI-Firmware-Security-Agent v0.6 IntegrationGateway adapter

> **派活方**: Claude
> **接收方**: Codex
> **前提**: 005-INTEG 已 done、006 v0.5 S4 已 done(93 passed + 1 skip)
> **优先级**: 🟢 Week 11

---

## ⚠️ 开工前必读(6 个文件)

1. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\adapters\base.py` —— adapter 契约
2. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\adapters\firmware.py` —— FirmwareAdapter 当前实现
3. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\gateway.py`
4. `E:\001项目\000开发\003AI+网络安全\000shared-llm-core\docs\v0.5-contract.md` §7
5. `E:\001项目\000开发\003AI+网络安全\006AI-Firmware-Security-Agent\docs\tech-spec.md`
6. `E:\001项目\000开发\003AI+网络安全\006AI-Firmware-Security-Agent\docs\TODO.md`

## ⚠️ Windows 踩坑经验

`E:\001项目\000开发\003AI+网络安全\AUDIT\EXPERIENCE.md` —— **经验 1 + 2 + 4**(matplotlib 已在 006 装好)

---

## 工作目录

```
E:\001项目\000开发\003AI+网络安全\006AI-Firmware-Security-Agent
```

## 当前状态

- NVD/EPSS/KEV 真实 API + binwalk/SquashFS 解包 + PRisk 公式 + 攻击链 + 0day 预测 + Docker + CI + 93 tests + 1 skip
- **缺**: CLI 子命令 `--input --json` 适配契约

---

## 本 Stage 目标(2 个 ISSUE / 2 个 commit)

### ISSUE 1 · FirmwareAdapter CLI 契约适配(FW-CLI-001)

**目标**: `python -m ai_firmware_agent.cli scan --input {...} --json` 被 adapter 调用,输出 envelope。

**任务**:
1. 查 `src/ai_firmware_agent/__main__.py` / cli.py
2. 新增 `python -m ai_firmware_agent.cli scan --input <payload> --json`:
   - payload: `{"firmware_path": "<absolute path>"}` 或 `{"firmware_url": "..."}`
   - `--json` 输出 envelope
3. 输出 envelope:
```json
{
  "findings": [
    {"id": "fw-cve-001", "severity": "critical", "confidence": 0.96,
     "title": "CVE-2024-1234 in busybox", "cve": "CVE-2024-1234",
     "host": "<firmware path>", "narrative": "PRisk 9.5, KEV listed"}
  ]
}
```

**测试**(≥ 3 个):
- `test_cli_envelope.py::test_scan_firmware_path`
- `test_cli_envelope.py::test_scan_corrupted_firmware_graceful`
- `test_cli_envelope.py::test_envelope_contains_prisk_narrative`
- 复用 `JSONSubprocessAdapter` end-to-end

**验收**:
- `echo '{"firmware_path":"samples/mini.bin"}' | python -m ai_firmware_agent.cli scan --json` → 合法 JSON
- pytest 全过
- 1 commit

### ISSUE 2 · 真实固件样本 + 集成(FW-LIVE-001)

**目标**: 跑通端到端 —— `/v0.5/firmware/scan` 触发固件分析,进 registry。

**任务**:
1. 新增 `samples/` 加 1 个公开小固件(≤ 10 MB,如从 `https://github.com/firmianay/firmwares` 拉一个 demo)
2. `tests/integration/test_firmware_adapter_e2e.py`:gateway 18080,POST `/v0.5/firmware/scan` + GET `/v0.5/health`
3. README 加章节"扫描固件"

**测试**(≥ 3 个):
- `test_firmware_adapter_e2e.py::test_post_scan_returns_findings`
- `test_firmware_adapter_e2e.py::test_health_endpoint_reports_firmware_ok`
- `test_cli.py::test_scan_real_firmware_sample`

**验收**:
- gateway 起来后 curl `/v0.5/health` 全 ok
- pytest 全过(93 + 1 skip + 新增 ≥ 6)
- 1 commit

---

## 约束

- 不修改 `000shared-llm-core` / `000shared-integration`
- 不破坏 v0.5 S4 的 93 + 1 skip
- 不装新依赖
- Windows 兼容,pytest 三件套
- samples/ 固件总和 ≤ 20 MB

---

## 回报

每 ISSUE 回报同上格式。最终 2 commit + 总测试数 + 联调输出。