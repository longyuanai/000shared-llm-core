# Codex 派活指令 · 003-S4 · AI-Agent-Security-Lab v0.6 IntegrationGateway adapter

> **派活方**: Claude
> **接收方**: Codex
> **前提**: 005-INTEG 已 done、003 v0.5 S3 已 done(140 passed)
> **优先级**: 🟢 Week 11

---

## ⚠️ 开工前必读(6 个文件)

1. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\adapters\base.py` —— adapter 契约
2. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\adapters\lab.py` —— LabAdapter 当前实现
3. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\gateway.py`
4. `E:\001项目\000开发\003AI+网络安全\000shared-llm-core\docs\v0.5-contract.md` §7 + §9 (MultiAgentOrchestrator)
5. `E:\001项目\000开发\003AI+网络安全\003AI Agent安全靶场\docs\tech-spec.md`
6. `E:\001项目\000开发\003AI+网络安全\003AI Agent安全靶场\docs\TODO.md`

## ⚠️ Windows 踩坑经验

`E:\001项目\000开发\003AI+网络安全\AUDIT\EXPERIENCE.md` —— **经验 1 + 2 + 4**

---

## 工作目录

```
E:\001项目\000开发\003AI+网络安全\003AI Agent安全靶场
```

## 当前状态

- 5 内置脆弱 Agent + 10 攻击场景 + ASR 评估 + 140 tests
- **缺**: CLI 子命令 `--input --json` 适配契约

---

## 本 Stage 目标(2 个 ISSUE / 2 个 commit)

### ISSUE 1 · LabAdapter CLI 契约适配(LAB-CLI-001)

**目标**: `python -m ai_agent_lab.cli scan --input {...} --json` 被 adapter 调用,输出 envelope。

**任务**:
1. 查 `src/ai_agent_lab/__main__.py` / cli.py
2. 新增 `python -m ai_agent_lab.cli scan --input <payload> --json`:
   - payload: `{"agent": "sql_assistant|email_assistant|file_rag|web_browser|code_act", "attack": "indirect_inj|token_steal|shell_escape|...", "iterations": 1}`
   - 输出 findings:每个 attack 触发后 detector 命中结果 → 1 个 finding
3. 输出 envelope:
```json
{
  "findings": [
    {"id": "lab-sqli-001", "severity": "high", "confidence": 0.88,
     "title": "SQL injection via indirect prompt injection",
     "narrative": "Agent: sql_assistant, Attack: indirect_injection, ASR=1/1"}
  ]
}
```

**测试**(≥ 3 个):
- `test_cli_envelope.py::test_scan_runs_attack_returns_finding`
- `test_cli_envelope.py::test_scan_handles_blocked_attack_gracefully`
- `test_cli_envelope.py::test_scan_invalid_attack_returns_empty_findings`
- 复用 `JSONSubprocessAdapter` end-to-end

**验收**:
- `echo '{"agent":"sql_assistant","attack":"indirect_injection"}' | python -m ai_agent_lab.cli scan --json` → 合法 JSON
- pytest 全过
- 1 commit

### ISSUE 2 · 真实 LLM 切换 + 集成(LAB-LIVE-001)

**目标**: 让 `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` 环境变量决定 LLM(无 key 时 fallback 到 fake),跑通端到端。

**任务**:
1. `src/ai_agent_lab/orchestrator.py` 加 env 读取(`LLM_PROVIDER=openai|anthropic|fake`)
2. 无 key / fake 模式:用现有 fake LLM(已有)
3. openai 模式:`pip install openai` 是新增依赖,**派活文档授权装**
4. `tests/integration/test_lab_adapter_e2e.py`:gateway 18080,POST `/v0.5/lab/scan` + GET `/v0.5/health`
5. README 加章节"LLM provider 切换"

**测试**(≥ 3 个):
- `test_lab_adapter_e2e.py::test_post_scan_returns_findings`
- `test_lab_adapter_e2e.py::test_health_endpoint_reports_lab_ok`
- `test_orchestrator.py::test_fake_llm_fallback_when_no_key`

**验收**:
- gateway 起来后 curl `/v0.5/health` 全 ok
- pytest 全过
- 1 commit

---

## 约束

- 不修改 `000shared-llm-core` / `000shared-integration`
- 不破坏 v0.5 S3 的 140 测试
- 本 Stage **授权装** `openai` 包(其他不开)
- Windows 兼容,pytest 三件套

---

## 回报

每 ISSUE 回报同上格式。最终 2 commit + 总测试数 + 联调输出。