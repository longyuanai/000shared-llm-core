# Codex 派活指令 · 005-S4 · AI-Reverse-Agent v0.6 IntegrationGateway adapter

> **派活方**: Claude
> **接收方**: Codex
> **前提**: 005-INTEG 已 done、005 v0.5 S3 已 done(150 passed)
> **优先级**: 🟢 Week 11

---

## ⚠️ 开工前必读(6 个文件)

1. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\adapters\base.py` —— adapter 契约
2. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\adapters\reverse.py` —— ReverseAdapter 当前实现
3. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\gateway.py`
4. `E:\001项目\000开发\003AI+网络安全\000shared-llm-core\docs\v0.5-contract.md` §7
5. `E:\001项目\000开发\003AI+网络安全\005AI逆向Agent\docs\tech-spec.md`
6. `E:\001项目\000开发\003AI+网络安全\005AI逆向Agent\docs\TODO.md`

## ⚠️ Windows 踩坑经验

`E:\001项目\000开发\003AI+网络安全\AUDIT\EXPERIENCE.md` —— **经验 1 + 2 + 4**(capstone 在 005 里是已知 dep,装包命令 `pip install capstone`)

---

## 工作目录

```
E:\001项目\000开发\003AI+网络安全\005AI逆向Agent
```

## 当前状态

- 6 架构(x86/x64/ARM/AArch64/MIPS/RISC-V)+ Capstone 反汇编 + Ghidra P-Code + FLIRT + 控制流图 + 150 tests
- **缺**: CLI 子命令 `--input --json` 适配契约

---

## 本 Stage 目标(2 个 ISSUE / 2 个 commit)

### ISSUE 1 · ReverseAdapter CLI 契约适配(REV-CLI-001)

**目标**: `python -m ai_reverse_agent.cli scan --input {...} --json` 被 adapter 调用,输出 envelope。

**任务**:
1. 查 `src/ai_reverse_agent/__main__.py` / cli.py
2. 新增 `python -m ai_reverse_agent.cli scan --input <payload> --json`:
   - payload: `{"binary_path": "<absolute path>", "arch": "x86|x64|arm|aarch64|mips|riscv"}`
   - `--json` 输出 envelope
3. 输出 envelope:
```json
{
  "findings": [
    {"id": "rev-bin-001", "severity": "high", "confidence": 0.85,
     "title": "Hardcoded credentials at 0x4012A0", "host": "<binary path>",
     "narrative": "Decompiled string: 'admin:password123'"}
  ]
}
```
   - `host` 字段在 reverse 场景里表示 binary 路径(对齐 Finding schema)

**测试**(≥ 3 个):
- `test_cli_envelope.py::test_scan_x64_binary`
- `test_cli_envelope.py::test_scan_arm_binary`
- `test_cli_envelope.py::test_scan_handles_unsupported_arch_gracefully`
- 复用 `JSONSubprocessAdapter` end-to-end

**验收**:
- `echo '{"binary_path":"samples/mini_pe.exe","arch":"x64"}' | python -m ai_reverse_agent.cli scan --json` → 合法 JSON
- pytest 全过
- 1 commit

### ISSUE 2 · 多架构真实样本 + 集成(REV-LIVE-001)

**目标**: 跑通端到端 —— `/v0.5/reverse/scan` 触发二进制扫描,进 registry。

**任务**:
1. 新增 `samples/mini_binaries/`(≥ 3 个真实小二进制:< 1 MB,合法可分析):x64 PE / ARM ELF / MIPS ELF
2. `tests/integration/test_reverse_adapter_e2e.py`:gateway 18080,POST `/v0.5/reverse/scan` + GET `/v0.5/health`
3. README 加章节"扫描二进制"

**测试**(≥ 3 个):
- `test_reverse_adapter_e2e.py::test_post_scan_returns_findings`
- `test_reverse_adapter_e2e.py::test_health_endpoint_reports_reverse_ok`
- `test_cli.py::test_scan_all_archs_in_samples`

**验收**:
- gateway 起来后 curl `/v0.5/health` 全 ok
- pytest 全过(150 + 新增 ≥ 6)
- 1 commit

---

## 约束

- 不修改 `000shared-llm-core` / `000shared-integration`
- 不破坏 v0.5 S3 的 150 测试
- 不装新依赖(capstone 已装)
- Windows 兼容,pytest 三件套
- samples/ 二进制总和 ≤ 5 MB

---

## 回报

每 ISSUE 回报同上格式。最终 2 commit + 总测试数 + 联调输出。