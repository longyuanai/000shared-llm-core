# Codex 派活指令 · 004-S4 · AI-CodeGuard-upgrade v0.6 IntegrationGateway adapter

> **派活方**: Claude
> **接收方**: Codex
> **前提**: 005-INTEG 已 done、004 v0.5 S3 已 done(55 passed,AI-CodeGuard-main 572 tests 不破坏)
> **优先级**: 🟢 Week 11

---

## ⚠️ 开工前必读(6 个文件)

1. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\adapters\base.py` —— adapter 契约
2. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\adapters\code.py` —— CodeAdapter 当前实现
3. `E:\001项目\000开发\003AI+网络安全\000shared-integration\src\shared_integration\gateway.py`
4. `E:\001项目\000开发\003AI+网络安全\000shared-llm-core\docs\v0.5-contract.md` §7
5. `E:\001项目\000开发\003AI+网络安全\004AI代码审计\004AI-CodeGuard-upgrade\docs\tech-spec.md`
6. `E:\001项目\000开发\003AI+网络安全\004AI代码审计\004AI-CodeGuard-upgrade\docs\TODO.md`

## ⚠️ Windows 踩坑经验(三件套 + tree-sitter cp314!)

`E:\001项目\000开发\003AI+网络安全\AUDIT\EXPERIENCE.md` —— **经验 1 + 2 + 3**(尤其经验 3:004 的 tree-sitter binding 必须 cp314,跑测试前 verify)

---

## 工作目录

```
E:\001项目\000开发\003AI+网络安全\004AI代码审计\004AI-CodeGuard-upgrade
```

## 当前状态

- tree-sitter 多语言(Python/Go/Java)+ 规则引擎 + CI 集成 + 55 tests
- **缺**: CLI 子命令 `--input --json` 适配契约

---

## 本 Stage 目标(2 个 ISSUE / 2 个 commit)

### ISSUE 1 · CodeAdapter CLI 契约适配(CODE-CLI-001)

**目标**: `python -m ai_codeguard.cli scan --input {...} --json` 被 adapter 调用,输出 envelope。

**任务**:
1. 查 `src/ai_codeguard/__main__.py` / cli.py
2. 新增 `python -m ai_codeguard.cli scan --input <payload> --json`:
   - payload: `{"repo_path": "<absolute path>", "languages": ["python","go","java"], "rules": [...]}` 或 `{"git_url": "..."}`
   - `--json` 输出 envelope
3. 输出 envelope(必含字段对齐 Finding):
```json
{
  "findings": [
    {"id": "code-py-001", "severity": "high", "confidence": 0.87,
     "title": "SQL injection in db.py:42", "host": "<file path>",
     "narrative": "Tainted user input flows to cursor.execute"}
  ]
}
```

**测试**(≥ 3 个):
- `test_cli_envelope.py::test_scan_local_repo`
- `test_cli_envelope.py::test_scan_with_rules_filter`
- `test_cli_envelope.py::test_scan_unsupported_language_graceful`
- 复用 `JSONSubprocessAdapter` end-to-end
- **每个测试都先 verify `import tree_sitter._binding` OK(cp314)**

**验收**:
- `echo '{"repo_path":"samples/mini_repo"}' | python -m ai_codeguard.cli scan --json` → 合法 JSON
- pytest 全过
- 1 commit

### ISSUE 2 · 真实 Git 接入 + 集成(CODE-LIVE-001)

**目标**: 支持 `git_url`(浅 clone)+ 跑通端到端。

**任务**:
1. `src/ai_codeguard/cli.py` 加 `--git-url <url>` 参数(用 `git clone --depth 1` 到临时目录)
2. 离线/网络失败时 fallback 到本地 `--repo-path`
3. `tests/integration/test_code_adapter_e2e.py`:gateway 18080,POST `/v0.5/code/scan` body `{"repo_path": "..."}` + GET `/v0.5/health`
4. README 加章节"扫描本地仓库 / Git URL"

**测试**(≥ 3 个):
- `test_code_adapter_e2e.py::test_post_scan_returns_findings`
- `test_code_adapter_e2e.py::test_health_endpoint_reports_code_ok`
- `test_cli.py::test_git_url_clone_failure_falls_back`

**验收**:
- gateway 起来后 curl `/v0.5/health` 全 ok
- pytest 全过(含 tree-sitter binding 验证)
- 1 commit

---

## 约束

- **不要**碰 `004AI代码审计\AI-CodeGuard-main`(上游 572 测试绝不能破坏)
- **不要**修改 `000shared-llm-core` / `000shared-integration`
- 不破坏 v0.5 S3 的 55 测试
- **不装新依赖**(tree-sitter 已 bundled)
- 跑测试前**必先** verify:`python -c "import tree_sitter._binding; print('OK')"`
- Windows 兼容,pytest 三件套

---

## 回报

每 ISSUE 回报同上格式。最终 2 commit + 总测试数(55 + 新增 ≥ 6,572 上游不破) + 联调输出。